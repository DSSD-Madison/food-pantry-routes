# Setup
```
make venv
make deps
```

Make sure to add the .env file in the backend directory for Supabase integration (check Google doc).

# Run
```
source backend/.venv/bin/activate
make backend
```
Second terminal: 
```
make frontend
```
